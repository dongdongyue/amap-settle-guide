# 高德开放平台 API 配置
# 获取方式：https://lbs.amap.com/ → 控制台 → 创建应用

# 必填：Web 服务 API Key（用于地理编码、POI 搜索、路径规划）
# 控制台 → 添加 Key → 类型选「Web 服务」
AMAP_API_KEY=

# 可选：Web端(JS API) Key + 安全密钥（用于生成交互地图）
# 控制台 → 添加 Key → 类型选「Web端(JS API)」
# 不配置时自动降级为纯 CSS 文字卡片
AMAP_JSAPI_KEY=
AMAP_SECURITY_JS_CODE=
